# hashicorp-vault-ui-ts service
apiVersion: v1
kind: Service
metadata:
  name: hashicorp-vault-ui-ts
  namespace: platform
spec:
  selector:
    app.kubernetes.io/name: vault
  ports:
    - name: https
      port: 8200
      targetPort: 8200
  type: ClusterIP