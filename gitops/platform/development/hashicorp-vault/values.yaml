injector:
  enabled: false

global:
  enabled: true
  tlsDisable: false
  resources:
    requests:
      memory: 256Mi
      cpu: 250m
    limits:
      memory: 256Mi
      cpu: 250m

# Vault UI
ui:
  enabled: true
  serviceType: "LoadBalancer"
  serviceNodePort: null
  externalPort: 8200

server:
  # roll pods automatically when spec changes
  updateStrategyType: RollingUpdate

  # extraVolumes is a list of extra volumes to mount. These will be exposed
  # to Vault in the path `/vault/userconfig/<name>/`.
  extraVolumes:
    - type: secret
      name: vault-server-tls
    - type: secret
      name: vault-internal-ca

  # This configures the Vault Statefulset to create a PVC for audit logs.
  # See https://www.vaultproject.io/docs/audit/index.html to know more
  #auditStorage:
  #  enabled: true

  # mount cert-manager's TLS secret
  #volumes:
  #  - name: userconfig-vault-server-tls
  #    secret:
  #      secretName: vault-server-tls

  #volumeMounts:
  #  - name: userconfig-vault-server-tls
  #    mountPath: /vault/userconfig/vault-server-tls
  #    readOnly: true

  # make CLI/probes trust the CA by default
  extraEnvironmentVars:
    VAULT_CACERT: /vault/userconfig/vault-internal-ca/ca.crt

  # For HA configuration and because we need to manually init the vault,
  # we need to define custom readiness/liveness Probe settings
  readinessProbe:
    enabled: true
    path: "/v1/sys/health?standbyok=true&sealedcode=204&uninitcode=204"
  livenessProbe:
    enabled: true
    path: "/v1/sys/health?standbyok=true"
    initialDelaySeconds: 120

  # IMPORTANT: tell Traefik to use HTTPS upstream + your ServersTransport
  service:
    annotations:
      traefik.ingress.kubernetes.io/service.serversscheme: https
      traefik.ingress.kubernetes.io/service.serverstransport: ingress-traefik-vault-tls@kubernetescrd

  # Single node standalone mode
  standalone:
    enabled: true
    config: |
      ui = true
      listener "tcp" {
        address = "[::]:8200"
        tls_cert_file = "/vault/userconfig/vault-server-tls/tls.crt"
        tls_key_file  = "/vault/userconfig/vault-server-tls/tls.key"
      }
      storage "file" {
        path = "/vault/data"
      }

  dataStorage:
    enabled: true
    size: 10Gi
    accessMode: ReadWriteOnce
    storageClass: hcloud-volumes
    annotations:
      "helm.sh/resource-policy": keep

  resources:
    requests:
      memory: 256Mi
      cpu: 250m
    limits:
      memory: 500Mi
      cpu: 500m