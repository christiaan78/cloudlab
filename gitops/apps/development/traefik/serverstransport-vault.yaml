apiVersion: traefik.io/v1alpha1
kind: ServersTransport
metadata:
  name: vault-tls
  namespace: ingress-traefik
spec:
  # Use the DNS SAN that’s on Vault’s cert
  serverName: hashicorp-vault.platform.svc
  # Traefik v3+: use rootCAs (not rootCAsSecrets)
  rootCAs:
    - secret: vault-ca
