apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: envoy-gateway
  namespace: envoy-gateway-system
spec:
  interval: 5m0s
  chart:
    spec:
      chart: gateway-helm
      version: "v1.7.x"
      sourceRef:
        kind: HelmRepository
        name: envoy-gateway
        namespace: envoy-gateway-system
  install:
    crds: Create
  upgrade:
    crds: CreateReplace